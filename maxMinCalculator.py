from utils import get_max,get_min

numbers = [2,3,8,7,10,1,2]

min_number = get_min(numbers)
print('min number is: ',min_number, max(numbers))
max_number = get_max(numbers)
print('max number is: ',max_number, min(numbers))